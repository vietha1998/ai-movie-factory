---
description: Trạng thái xưởng phim — project, stage, blocker, việc tiếp theo
---
Đọc `projects/*/project_state.json` (bỏ project PAUSED trừ khi được hỏi) và `git log --oneline -5`. Trả lời bằng bảng ngắn: từng tập (kịch bản/VEO/đóng gói/ảnh/video/audio/edit), blockers, `next`. Không chạy agent. Không sửa file.
